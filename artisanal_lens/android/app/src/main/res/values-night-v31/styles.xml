<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ replaced the window-background splash with the
         SplashScreen API, which draws the launcher icon on its own colour.
         Left alone that shows the default Flutter logo on white, which reads
         as a different app and then flashes into the cream opening sequence.
         Both are pinned to the app's own ground here. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/launch_background</item>
    </style>
</resources>
